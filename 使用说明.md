# 辰星 RSS 阅读器 — 使用说明

> 版本：v0.1.2 · Windows · 跨平台智能 RSS 阅读器

---

## 一、快速开始

1. 解压 `Chenxing-Windows-v0.1.2.zip` 到任意目录
2. 进入 `Chenxing` 文件夹，双击 **Chenxing.exe**
3. 如 Windows Defender 弹出提示，点击「更多信息」→「仍要运行」
4. 首次启动会自动创建本地数据库（`C:\Users\<用户名>\AppData\Local\Mercury\`）

---

## 二、界面概览

```
┌─────────────┬────────────────┬──────────────────────┐
│  订阅源列表  │    文章列表     │      阅读区域         │
│             │               │                      │
│  + 添加订阅  │  🔍 搜索框    │  [Reader│Web│双栏]   │
│  📂 文件夹   │  标题 · 时间   │  文章正文 / 网页      │
│  ⭐ 收藏夹   │  已读/未读标记  │  ✨ AI 摘要面板       │
│             │               │  🌐 AI 翻译工具栏     │
└─────────────┴────────────────┴──────────────────────┘
```

- **快捷键**：菜单栏 **AI → AI 设置**，或 `Ctrl+,` 打开设置

---

## 三、添加订阅源

1. 点击左侧顶部的 **「＋ 添加订阅」**
2. 输入 RSS / Atom 地址，例如：

| 来源 | 地址 |
|------|------|
| 少数派 | `https://sspai.com/feed` |
| 阮一峰的网络日志 | `https://feeds.feedburner.com/ruanyifeng` |
| 36氪 | `https://36kr.com/feed` |
| Hacker News | `https://hnrss.org/frontpage` |

3. 点击确定，系统自动拉取最新文章

> **OPML 导入导出**：菜单栏 **文件 → 导入 OPML / 导出 OPML**，可批量迁移订阅。

---

## 四、阅读文章

### 三种阅读模式

工具栏可随时切换：

| 模式 | 说明 |
|------|------|
| **Reader** | AI 正文提取 + 清洗排版，纯净阅读体验 |
| **Web** | 内置浏览器渲染原始网页 |
| **双栏** | 左右分屏，同时查看 Reader 和 Web 两种视图 |

### 文章操作

| 操作 | 方式 |
|------|------|
| 🔍 搜索 | 文章列表顶部的搜索框，支持全文搜索 |
| ✅ 已读/未读 | 选中文章即自动标记已读；右键 → 标记未读 |
| ⭐ 收藏 | 右键 → 收藏，收藏夹在左侧栏可查看 |
| 🗑️ 删除 | 右键 → 删除（软删除，可恢复） |

### 显示设置

工具栏右侧可调节：
- **字号**：14 – 24 px
- **主题**：浅色 / 深色 / 跟随系统
- **内容宽度**：窄 / 中 / 宽

### 专注模式

点击工具栏右侧 **⛶** 按钮，隐藏侧栏和文章列表，阅读区占满全窗口。再次点击恢复。

---

## 五、AI 功能配置

Chenxing 内置三个 AI 智能体：**摘要**、**翻译**、**标签推荐**。使用前需先配置 LLM。

### 第一步：配置 LLM 服务商

1. 菜单栏 **AI → AI 设置**
2. 切换到 **Provider** 标签页
3. 填写以下信息：

| 服务商 | API Base URL | 模型示例 | API Key |
|--------|-------------|----------|---------|
| 本地 Ollama | `http://localhost:11434/v1` | `qwen3` / `llama3.2` | 留空即可 |
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` / `gpt-4o` | `sk-xxx` |
| 其他兼容 API | 自定义地址 | 自定义 | 按服务商提供 |

4. 点击 **测试连接** 验证配置是否正确


### 第二步：配置智能体

切换到 **Agent** 标签页：

| 设置项 | 说明 |
|--------|------|
| 摘要语言 | 生成摘要的语言（中文 / 英文） |
| 摘要详细度 | 简短 / 标准 / 详细 |
| 翻译目标语言 | 翻译为哪种语言 |
| 翻译并发数 | 同时翻译的分段数（越大越快，但可能触发限流） |
| 自动摘要 | 开启后打开文章自动生成摘要 |

### 第三步：使用 AI 功能

| 功能 | 操作 |
|------|------|
| **AI 摘要** | 打开文章 → 底部「✨ AI 摘要」→ 点击「生成摘要」，流式显示结果 |
| **AI 翻译** | 阅读工具栏点「翻译」→ 实时显示进度 → 完成后可选 **原文 / 双语 / 仅译文** |
| **AI 标签** | 

---

## 六、数据存储

所有数据存于 `C:\Users\<用户名>\.chenxing\`：

```
.chenxing\
├── chenxing.db          # SQLite 数据库（订阅、文章、标签、设置）
└── translation_*.html   # 翻译导出缓存
```

| 操作 | 方法 |
|------|------|
| **备份** | 复制整个 `.chenxing` 文件夹 |
| **迁移** | 将备份文件夹放回新电脑 `C:\Users\<用户名>\` 下 |
| **重置** | 删除 `.chenxing` 文件夹即可恢复出厂状态 |

---

## 七、命令行工具（高级）

在 `Chenxing` 目录下打开终端/CMD：

```cmd
# 配置 LLM
Chenxing.exe configure --base-url http://localhost:11434/v1 --model qwen3

# 列出所有文章
Chenxing.exe list

# 单独测试 AI 功能
Chenxing.exe summary 1     # 对第 1 篇文章生成摘要
Chenxing.exe translate 1   # 对第 1 篇文章翻译
Chenxing.exe tagging 1     # 对第 1 篇文章推荐标签

# 一键验证所有 Agent
Chenxing.exe verify-all 1
```

---

## 八、常见问题

**Q: 双击 exe 无反应？**
确认杀毒软件未隔离文件。可尝试在 exe 所在目录打开 CMD，输入 `Chenxing.exe` 回车，查看报错信息。

**Q: 文章显示「Reader 模式不可用」？**
部分网站（如 Cloudflare 防护站点）屏蔽了抓取请求，系统会自动降级为 Web 模式用内置浏览器渲染。

**Q: AI 功能点击无响应？**
请先确认已在 **AI → AI 设置** 中配置了 LLM Provider，并点「测试连接」验证。

**Q: 如何批量导入订阅？**
菜单栏 **文件 → 导入 OPML**，选择 `.opml` 文件即可。大多数 RSS 阅读器都支持 OPML 导出。

**Q: 翻译太慢？**
在 AI 设置的 Agent 标签页调高「翻译并发数」，或切换到更快的模型。

**Q: API Key 存在哪里？安全吗？**
API Key 使用 Windows 系统级密钥链（Credential Manager）加密存储，不会明文写入数据库或配置文件。

---

## 九、快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+,` | 打开 AI 设置 |
| `Ctrl+F` | 文章搜索 |
| `Ctrl+R` | 刷新当前订阅源 |
| `F11` | 全屏模式 |

---

## 十、技术栈

| 层 | 技术 |
|----|------|
| UI 框架 | PySide6 + Qt WebEngine |
| 正文提取 | readability-lxml + BeautifulSoup |
| AI Agent | OpenAI 兼容 API（支持 Ollama / GPT / DeepSeek） |
| 数据库 | SQLite（WAL 模式） |
| 密钥存储 | Windows Credential Manager |

---

<div align="center">

**辰星 RSS 阅读器** · 华东师范大学 2026 夏季学期软件开发小组项目

</div>
