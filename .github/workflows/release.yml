name: Build Packages

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      platform:
        description: "Target platform"
        required: true
        type: choice
        options: [macos, windows, all]
        default: macos
      create_release:
        description: "Publish to GitHub Releases"
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  # ── macOS .dmg ────────────────────────────────────────────────────────
  build-macos:
    if: ${{ github.event_name == 'push' || inputs.platform == 'macos' || inputs.platform == 'all' }}
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pyinstaller
          pip install -e ".[dev]"

      - name: Build macOS .app
        run: |
          pyinstaller mercury.spec

      - name: Package as DMG
        run: |
          APP="dist/Mercury.app"
          DMG="dist/Mercury-macos-${GITHUB_REF_NAME:-snapshot}.dmg"
          mkdir -p dist/dmg
          cp -R "$APP" dist/dmg/
          cp RELEASE_README.md dist/dmg/使用指南.md 2>/dev/null || true
          ln -s /Applications dist/dmg/Applications
          hdiutil create -volname "Mercury" -srcfolder dist/dmg \
            -ov -format UDZO "$DMG"
          echo "DMG=$DMG" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mercury-macos
          path: dist/Mercury-macos-*.dmg

      - name: Publish GitHub Release
        if: ${{ inputs.create_release || startsWith(github.ref, 'refs/tags/') }}
        uses: softprops/action-gh-release@v2
        with:
          files: dist/Mercury-macos-*.dmg
          generate_release_notes: true

  # ── Windows .exe ──────────────────────────────────────────────────────
  build-windows:
    if: ${{ github.event_name == 'push' || inputs.platform == 'windows' || inputs.platform == 'all' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pyinstaller
          pip install -e ".[dev]"

      - name: Build Windows .exe
        run: |
          pyinstaller mercury.spec

      - name: Create ZIP package
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME:-snapshot}"
          ZIP="Mercury-windows-${VERSION}.zip"
          zip -9 -r "${ZIP}" dist/Mercury/
          echo "ZIP=${ZIP}" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mercury-windows
          path: Mercury-windows-*.zip

      - name: Publish GitHub Release
        if: ${{ inputs.create_release || startsWith(github.ref, 'refs/tags/') }}
        uses: softprops/action-gh-release@v2
        with:
          files: Mercury-windows-*.zip
          generate_release_notes: true
